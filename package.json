{
  "name": "downloader",
  "version": "1.0.0",
  "description": "A simple HTTP file downloader with progress bar",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leo Ackerman",
  "license": "ISC",
  "dependencies": {
    "request": "^2.65.0"
  }
}
